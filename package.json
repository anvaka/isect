{
  "name": "isect",
  "version": "3.0.3",
  "main": "build/isect.js",
  "module": "build/isect.module.js",
  "types": "./index.d.ts",
  "scripts": {
    "pretest": "rollup -c",
    "test": "tap --disable-coverage test/*.js",
    "start": "rm -rf build && rollup -c && rollup -c rollup.config-min.mjs",
    "prepublishOnly": "npm run start"
  },
  "dependencies": {
    "splaytree": "^2.0.2"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^29.0.0",
    "@rollup/plugin-node-resolve": "^16.0.0",
    "@rollup/plugin-terser": "^1.0.0",
    "benchmark": "^2.1.4",
    "bentley-ottman-sweepline": "^0.1.4",
    "flatbush": "^3.0.0",
    "ngraph.random": "^0.1.0",
    "rollup": "^4.0.0",
    "tap": "^21.0.0"
  }
}
